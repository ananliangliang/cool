ktor:
  application:
    modules:
      - io.github.ananliangliang.cool.ApplicationKt.module
  deployment:
    port: 8080
    rootPath: /api
jwt:
  secret: secret
  issuer: "https://qifeng.icu"
h2:
  url: jdbc:h2:./cool;AUTO_SERVER=TRUE
  user: ${DB_USERNAME}
  password: ${DB_PASSWORD}